============
Installation
============

At the command line::

    $ easy_install django-custom-email-user

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-custom-email-user
    $ pip install django-custom-email-user
